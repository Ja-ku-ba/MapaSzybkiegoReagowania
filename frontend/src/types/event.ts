export interface IEvents {
    latitiude: number,
    longitude: number,
    creator: string, 
    type: string, 
    created_at: string, 
    description: string | null,
    show_description: boolean
}